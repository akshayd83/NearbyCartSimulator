<resources>
    <attr name="selectedColor" format="color" />
    <attr name="unselectedColor" format="color" />
    <declare-styleable name="CirclePageIndicator">
        <!-- Whether or not the indicators should be centered. -->
        <attr name="centered" format="boolean"/>
        <!-- Color of the filled circle that represents the current page. -->
        <attr name="fillColor" format="color" />
        <!-- Color of the filled circles that represents pages. -->
        <attr name="pageColor" format="color" />
        <!-- Orientation of the indicator. -->
        <attr name="android:orientation"/>
        <!-- Radius of the circles. This is also the spacing between circles. -->
        <attr name="radius" format="dimension" />
        <!-- Whether or not the selected indicator snaps to the circles. -->
        <attr name="snap" format="boolean" />
        <!-- Color of the open circles. -->
        <attr name="strokeColor" format="color" />
        <!-- Width of the stroke used to draw the circles. -->
        <attr name="strokeWidth" format="dimension" />
        <!-- View background -->
        <attr name="android:background"/>
        <attr name="fadeOut" format="boolean" />
        <!-- Spacing between two circles, default = circle radius -->
        <attr name="spacing" format="dimension" />
        <!-- Shadow section -->
        <attr name="showShadow" format="boolean" />
        <attr name="shadowColor" format="color" />
        <attr name="shadowOffsetX" format="integer" />
        <attr name="shadowOffsetY" format="integer" />

    </declare-styleable>

        <bool name="default_circle_indicator_centered">true</bool>
        <color name="default_circle_indicator_fill_color">#3F51B5</color>
        <color name="default_circle_indicator_shadow_color">#ff646464</color>
        <color name="default_circle_indicator_page_color">#3F51B5</color>
        <integer name="default_circle_indicator_orientation">0</integer>
        <dimen name="default_circle_indicator_radius">3dp</dimen>
        <bool name="default_circle_indicator_snap">false</bool>
        <bool name="default_circle_indicator_fadeOut">false</bool>
        <bool name="default_circle_indicator_showShadow">false</bool>
        <color name="default_circle_indicator_stroke_color">#FFDDDDDD</color>
        <dimen name="default_circle_indicator_stroke_width">1dp</dimen>
        <integer name="default_circle_indicator_shadow_offset_x">0</integer>
        <integer name="default_circle_indicator_shadow_offset_y">0</integer>
 </resources>